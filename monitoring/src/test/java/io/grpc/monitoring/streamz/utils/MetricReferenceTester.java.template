// Generated by ${generator}

package io.grpc.monitoring.streamz.utils;

import io.grpc.monitoring.streamz.Field;

import javax.annotation.Generated;

/**
 * Reference to an ${number}-dimensional Metric. Used for testing.
 *
 * <p>See {@link StreamzTester} for factory methods.
 *
${type_javadoc} * @param <V> The type of the values stored in metric cells.
 * @author ecurran@google.com (Eoin Curran)
 * @author avrukin@google.com (Adopted / Moved to gRPC & Open Source)
*/
@Generated(value = "${generator}")
public class MetricReferenceTester${dimensionality}<${types}, V>
     extends MetricReferenceTester<V> {

  MetricReferenceTester${dimensionality}(StreamzTester tester, String metricName,
      Class<V> valueType,
      ${new_field_params}) {
    super(tester, metricName, valueType, ${fields});
  }

  MetricReferenceTester${dimensionality}(StreamzTester tester, String metricName, Class<V> valueType,
      String... fieldNames) {
    super(tester, metricName, valueType, fieldNames);
  }
}